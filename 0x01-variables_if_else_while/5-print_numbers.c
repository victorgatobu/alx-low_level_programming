#include <stdio.h>
/**
 * the main function - start of program
 * program to print single digit numbers
 * main function returns 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%i", n);
	}
	putchar('\n');

	return (0);
}
