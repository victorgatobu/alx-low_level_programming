#include <stdio.h>
/**
 * main - start of program
 * program to print single digit numbers
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%i", n);
	}
	putchar('\n');

	return (0);
}
